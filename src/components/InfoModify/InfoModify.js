import React from 'react';

import styled from 'styled-components';
import { StylesProvider } from '@material-ui/core';

// 1. 유저 데이터 서버에서 가져오기
// 2. 가져온 데이터 화면에 뿌려주기
// 3. 수정한 데이터 다시 서버로 보내주기

/* data list
id, name, password, type, auth, email
*/

const InfoModify = () => {
  return <div></div>;
};

export default InfoModify;
